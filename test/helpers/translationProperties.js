import translations from '../../translations/ui-authorization-roles/en';

const translationsProperties = [
  {
    prefix: 'ui-authorization-roles',
    translations,
  },
];

export default translationsProperties;
